name="dash"
aliases=("dash")
requires=()

version(){
	dpkg -s dash | grep -Po '\d+.\d+.\d+'
}
install(){
	apt install -y dash
}
pre_uninstall(){}
post_uninstall(){}
path(){}
compile(){}
run(){
	xargs -a args.args -d '\n' dash code.code < stdin.stdin
}