name="scala"
aliases=("scala" "sc")
requires=("java")

version(){
	scala -version | grep -Po '\d+.\d+.\d+'
}
install(){}
pre_uninstall(){}
post_uninstall(){}
path(){
	echo "$LBIN/scala3-3.0.0-M3/bin"
}
compile(){}
run(){
	#TODO: Needs JAVA_HOME from java pld
	
	cp code.code interim.scala
	xargs -a args.args -d '\n' scala interim.scala < stdin.stdin
}