name="c"
aliases=("c")
requires=()

version(){
	gcc --version | grep -Po '\d+.\d+.\d+'
}
install(){
	apt install -y gcc
}
pre_uninstall(){}
post_uninstall(){}
path(){}
compile(){
	gcc -std=c11 -o binary -x c code.code
}
run(){
	xargs -a args.args -d '\n' ./binary < stdin.stdin
}