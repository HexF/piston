name="perl"
aliases=("perl" "pl")
requires=()

version(){
	perl --version | grep x86 | grep -Po '\d+.\d+.\d+'
}
install(){
	apt install -y perl
}
pre_uninstall(){}
post_uninstall(){}
path(){}
compile(){}
run(){
	xargs -a args.args -d '\n' perl code.code < stdin.stdin
}