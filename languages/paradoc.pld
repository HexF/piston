name="paradoc"
aliases=("paradoc")
requires=("python3")

version(){
	python3.8 -m paradoc --version | grep -Po '\d+.\d+.\d+'
}
install(){
	git clone https://github.com/betaveros/paradoc.git
}
pre_uninstall(){}
post_uninstall(){}
path(){
	# TODO: Doesn't account for pythonpath
}
compile(){}
run(){
	export PYTHONPATH=$PYTHONPATH:$LBIN
	python -m paradoc code.code < args.args
}