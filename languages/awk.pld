name="awk"
aliases=("awk")
requires=()

version(){
	awk -W version | grep -Po '\d+.\d+.\d+'
}
install(){
	apt install -y gawk
}
pre_uninstall(){}
post_uninstall(){}
path(){}
compile(){}
run(){
	xargs -a args.args -d '\n' awk -f code.code < stdin.stdin
}